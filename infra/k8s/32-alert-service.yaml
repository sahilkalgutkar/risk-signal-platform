apiVersion: apps/v1
kind: Deployment
metadata:
  name: alert-service
  namespace: risk-signal
spec:
  replicas: 2
  selector:
    matchLabels:
      app: alert-service
  template:
    metadata:
      labels:
        app: alert-service
    spec:
      containers:
        - name: alert-service
          image: risk-signal-alert-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8083
          envFrom:
            - configMapRef:
                name: risk-signal-config
          env:
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: risk-signal-secrets
                  key: DB_PASSWORD
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8083
            initialDelaySeconds: 15
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8083
            initialDelaySeconds: 30
            periodSeconds: 20
          resources:
            requests:
              cpu: 250m
              memory: 384Mi
            limits:
              cpu: 1
              memory: 768Mi
---
apiVersion: v1
kind: Service
metadata:
  name: alert-service
  namespace: risk-signal
spec:
  selector:
    app: alert-service
  ports:
    - port: 8083
      targetPort: 8083
