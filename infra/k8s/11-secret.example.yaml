# Template only — do NOT apply this file as-is or commit real secrets.
#
# Create the real secret imperatively instead, e.g.:
#   kubectl create secret generic risk-signal-secrets -n risk-signal \
#     --from-literal=DB_PASSWORD="$(openssl rand -hex 16)" \
#     --from-literal=MYSQL_ROOT_PASSWORD="$(openssl rand -hex 16)"
#
# On EKS, prefer External Secrets Operator backed by AWS Secrets Manager
# instead of kubectl-managed Secrets, and pull RDS credentials from there.
apiVersion: v1
kind: Secret
metadata:
  name: risk-signal-secrets
  namespace: risk-signal
type: Opaque
stringData:
  DB_PASSWORD: "change-me"
  MYSQL_ROOT_PASSWORD: "change-me"
